schema {
    query: Query
}

type Query {
    getUserById(id:Int):User
    getMovieById(id:Int):Movie
}

type User {
    id: ID!
    name: String
    gender: String
}

type Movie {
    id:ID!
    name: String
    rating: Float
}